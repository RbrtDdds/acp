{
  "name": "@acp/mcp",
  "version": "0.1.0",
  "description": "AI Context Protocol — MCP server for Claude Code",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "acp-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@acp/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "license": "MIT"
}
